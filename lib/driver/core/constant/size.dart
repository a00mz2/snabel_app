int limit = 20;

/// ارتفاع [CustomBottomBarCustomer] — يُستخدم كـ `padding` سفلي للصفحات داخل [MainScreen]
/// حتى لا يُغطى المحتوى بالشريط.
const double kMainBottomNavigationBarHeight = 99;
// double widthScreen = Get.size.width;
// double heightScreen = Get.size.height;

    // double widthScreen = MediaQuery.of(context).size.width;
    // double heightScreen = MediaQuery.of(context).size.height;